
#define MINUS '-'
#define PLUS '+'
#define NUL '\0' 
#define SPACE ' '
#define DIV '/'

#define FALSE 0
#define TRUE 1
#define TAB '\t'
#define NEWLINE '\n'
#define BACKSPACE '\b'

#define max(a,b) ((a)>(b)) ? (a) : (b)  
#define istabpos(i) (tabpos[i-1] == 1 ) ? 1 : 0 
